#include "integrationClient.h"
#include "/home/amaresh/code/integration/inc/btree.hpp"

int main () {

    //Disable temporerly for direct testing
	//utility::start ();

	int arr [] = {15, 10, 100, 5, 13, 90, 80, 50, 10, 5, 3};
	//create an instance of btree::tree;
	//tree* tree = new tree();
	//tree->create_btree (arr);

	//now display tree
//	tree->display_btree_i (tree->get_root_node());

	return 0;
}

